<div class="container vh-100 d-flex justify-content-center">
  <div class="row align-items-center">
    <h1>New list</h1>
      <%= simple_form_for @list  do |f| %>
        <%= f.input :name %>
        <%= f.input :genre %>
        <%= f.submit "create"%>
      <% end %>
    <p>
      <a href="<%= lists_path %>" class="btn btn-primary m-4" style="text-decoration: none;">go back
      </a>
    </p>
  </div>
</div>
